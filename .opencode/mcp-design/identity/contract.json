{
  "contract_type": "all",
  "contracts": [
    {
      "type": "pre-condition",
      "description": "identity input validation",
      "assertion": "require(input !== null && input !== undefined)"
    },
    {
      "type": "pre-condition",
      "description": "identity authorization check",
      "assertion": "require(authorized === true)"
    },
    {
      "type": "post-condition",
      "description": "identity returns valid result",
      "assertion": "ensure(result !== null)"
    },
    {
      "type": "post-condition",
      "description": "identity no side effects on error",
      "assertion": "ensure(error === null || state_unchanged === true)"
    },
    {
      "type": "invariant",
      "description": "identity state consistency",
      "assertion": "invariant(state.version >= 0)"
    },
    {
      "type": "invariant",
      "description": "identity data integrity",
      "assertion": "invariant(checksum(data) === expected_checksum)"
    }
  ],
  "generatedAt": "2026-07-21T01:26:20.274Z"
}